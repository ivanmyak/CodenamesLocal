{
  "ConnectionStrings": {
    "KTCNamesDB": "Host=${POSTGRES_HOST};Port=${POSTGRES_PORT};Database=KTCNamesDB;Username=${POSTGRES_USER};Password=${POSTGRES_PASSWORD}"

  },
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  }
}
